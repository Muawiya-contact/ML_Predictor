# Copy to .env and fill in. .env is gitignored - never commit a real key.
#   cp .env.example .env
# Then export it before running translate_roman_urdu.py:
#   set -a; source .env; set +a
OPENROUTER_API_KEY=your-key-here
