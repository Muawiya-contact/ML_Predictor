# Python virtual env
.venv/
venv/
env/

# Byte-compiled / cache
__pycache__/
*.py[cod]

# Jupyter
.ipynb_checkpoints

# Hugging Face / sentence-transformers model cache (if downloaded into the project)
.cache/
hf_cache/
huggingface/
models--*/

# Generated prediction outputs (recreated by predict_batch.py)
*_predictions.csv
*_predictions.xlsx

# Captured console logs from local runs. The model artifacts in
# triage_model/ and the *_results.csv tables ARE tracked deliverables and
# must stay visible to git.
eval_out.txt
eval_raw.txt

# Editor temp
*.swp
*.swo

# OS noise
.DS_Store
Thumbs.db

# Scratch model snapshots and backup reports. Serving and research bundles
# are tracked; local rollback copies are not project deliverables.
triage_model_embedding_v*/
triage_model_embedding*.BACKUP*/
triage_model_embedding_PRE_FUZZYFIX/
learned_stopwords_v*.json
learned_stopwords_*BACKUP*.json

# Local environment settings and secrets.
.env
.env.*
!.env.example
*.key

# The supplied source archive is local only. Research code reads the
# extracted professor_baseline/ files, which are tracked.
How Effective is Embeddings Generator-*.zip
tests/fixtures/*_predictions.csv
tests/fixtures/*_predictions.xlsx
tests/fixtures/batch_empty*.csv
tests/fixtures/batch_empty*.xlsx
